from asyncio import get_event_loop
import threading
import time
from datetime import datetime

import ccxt
import ccxtpro


data = {'steps': 4, 'center': None, 'symbol': 'BTC/USDT', 'factor': 10000, 'threshold': 120, 'amount': 0.001}


def hftinit(data):




hftinit(data)
